import React from 'react'
import { Pokemon } from './PokemonProvider';

const Search = (props)=>{
    console.log(props)
    return(
        <div>
            Search
        </div>
    )
}
export default Pokemon(Search)